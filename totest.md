# To Test / Don't Forget

- Small text/label cleanup can usually be done without restarting the chain.
- Deep identity cleanup across core binaries, system contracts, and core config should be done with a controlled stop -> rebuild -> restart so everything stays consistent.
