import { Box } from '@pancakeswap/uikit'
import styled from 'styled-components'

/**
 * Wrapper for the expanded row liquidity distribution chart.
 * Keeps consistent height and spacing with the rest of the row.
 */
export const ChartContainer = styled(Box)`
  margin: 8px 0;
  min-height: 200px;
  width: 100%;
`
