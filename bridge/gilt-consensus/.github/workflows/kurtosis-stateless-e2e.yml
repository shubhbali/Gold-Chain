name: Kurtosis Stateless E2E Tests

on:
  push:
    branches:
      - 'main'
      - 'develop'
  pull_request:
    branches:
      - 'main'
      - 'develop'
    types: [opened, synchronize]

concurrency:
  group: kurtosis-stateless-e2e-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  ENCLAVE_NAME: kurtosis-stateless-e2e
  POLYCLI_VERSION: v0.1.103

jobs:
  build-gilt:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout gilt
        uses: actions/checkout@v5
        with:
          repository: 0xGilt/gilt
          ref: develop

      - name: Build gilt docker image
        run: docker build -t gilt:local --file Dockerfile .

      - name: Save gilt docker image
        run: docker save gilt:local | gzip > gilt-image.tar.gz

      - name: Upload gilt docker image
        uses: actions/upload-artifact@v4
        with:
          name: gilt-image
          path: gilt-image.tar.gz
          retention-days: 1

  build-giltconsensus-v2:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout giltconsensus-v2
        uses: actions/checkout@v5

      - name: Build giltconsensus-v2 docker image
        run: docker build -t giltconsensus-v2:local --file Dockerfile .

      - name: Save giltconsensus-v2 docker image
        run: docker save giltconsensus-v2:local | gzip > giltconsensus-v2-image.tar.gz

      - name: Upload giltconsensus-v2 docker image
        uses: actions/upload-artifact@v4
        with:
          name: giltconsensus-v2-image
          path: giltconsensus-v2-image.tar.gz
          retention-days: 1

  e2e-tests:
    needs:
      - build-gilt
      - build-giltconsensus-v2
    runs-on: ubuntu-latest
    timeout-minutes: 45
    # These permissions are required to log in to GCR
    permissions:
      contents: read
      actions: write
      id-token: write
    steps:
      # Set up kurtosis
      - name: Checkout kurtosis-pos
        uses: actions/checkout@v5
        with:
          repository: 0xGilt/kurtosis-pos
          ref: hv2/eip1559-and-rio-at-128

      # This step will free disk space and thus remove any docker images previously built
      - name: Pre kurtosis run
        uses: ./.github/actions/setup
        with:
          main_branch: develop
          docker_username: ${{ secrets.DOCKERHUB }}
          docker_token: ${{ secrets.DOCKERHUB_KEY }}

      - name: Checkout pos-workflows
        uses: actions/checkout@v5
        with:
          repository: 0xGilt/pos-workflows
          ref: main
          path: pos-workflows

      - name: Copy kurtosis config
        run: cp ./pos-workflows/configs/kurtosis-stateless-e2e.yml ./kurtosis-stateless-e2e.yml

      # Load images
      - name: Download gilt docker image
        uses: actions/download-artifact@v4
        with:
          name: gilt-image

      - name: Download giltconsensus-v2 docker image
        uses: actions/download-artifact@v4
        with:
          name: giltconsensus-v2-image

      - name: Load gilt docker image
        run: |
          gunzip -c gilt-image.tar.gz | docker load
          echo "Loaded gilt docker image:"
          docker images gilt:local --format "{{.ID}} {{.CreatedAt}}"

      - name: Load giltconsensus-v2 docker image
        run: |
          gunzip -c giltconsensus-v2-image.tar.gz | docker load
          echo "Loaded giltconsensus-v2 docker image:"
          docker images giltconsensus-v2:local --format "{{.ID}} {{.CreatedAt}}"

      # Deploy kurtosis enclave
      - name: Kurtosis run
        run: kurtosis run --args-file=kurtosis-stateless-e2e.yml --enclave ${{ env.ENCLAVE_NAME }} .

      - name: Inspect enclave
        run: kurtosis enclave inspect ${{ env.ENCLAVE_NAME }}

      # Run e2e tests
      - name: Install polycli
        run: |
          tmp_dir=$(mktemp -d)
          curl -L https://github.com/0xGilt/gilt-cli/releases/download/${{ env.POLYCLI_VERSION }}/polycli_${{ env.POLYCLI_VERSION }}_linux_amd64.tar.gz | tar -xz -C "$tmp_dir"
          mv "$tmp_dir"/* /usr/local/bin/polycli
          rm -rf "$tmp_dir"
          sudo chmod +x /usr/local/bin/polycli
          polycli version

      - name: Run stateless e2e tests
        id: stateless-tests
        working-directory: pos-workflows/tests/stateless_tests
        run: bash kurtosis_stateless_test.sh

      # Collect diagnostics on failure
      - name: Collect network diagnostics and state dump
        if: always() && steps.stateless-tests.outcome == 'failure'
        uses: ./pos-workflows/.github/actions/network-diagnostics-and-state-dump
        with:
          enclave_name: ${{ env.ENCLAVE_NAME }}

      # Clean up
      - name: Post kurtosis run
        if: always()
        uses: ./.github/actions/cleanup
        with:
          main_branch: develop
          enclave_name: ${{ env.ENCLAVE_NAME }}
