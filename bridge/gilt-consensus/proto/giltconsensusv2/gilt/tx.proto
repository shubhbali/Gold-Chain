syntax = "proto3";
package giltconsensusv2.gilt;

import "amino/amino.proto";
import "cosmos/msg/v1/msg.proto";
import "cosmos_proto/cosmos.proto";
import "gogoproto/gogo.proto";
import "giltconsensusv2/gilt/gilt.proto";

option go_package = "github.com/giltchain/gilt-consensus/x/gilt/types";

// Msg defines the gRPC message service for the gilt module.
service Msg {
  option (cosmos.msg.v1.service) = true;
  // ProposeSpan defines a method for proposing a new gilt span.
  rpc ProposeSpan(MsgProposeSpan) returns (MsgProposeSpanResponse);
  // UpdateParams defines a method to update the gilt module parameters.
  // Only the governance authority can execute this.
  rpc UpdateParams(MsgUpdateParams) returns (MsgUpdateParamsResponse);
  // BackfillSpans defines a method to backfill missing spans.
  // This is used during chain recovery or when spans need to be reconstructed.
  rpc BackfillSpans(MsgBackfillSpans) returns (MsgBackfillSpansResponse);
  // VoteProducers defines a method for validators to submit their producer
  // votes.
  rpc VoteProducers(MsgVoteProducers) returns (MsgVoteProducersResponse);
  // SetProducerDowntime defines a method to set planned downtime for a
  // producer. Producers can signal maintenance windows during which they won't
  // produce blocks.
  rpc SetProducerDowntime(MsgSetProducerDowntime)
      returns (MsgSetProducerDowntimeResponse);
}

// MsgProposeSpan defines the message for proposing a new span.
message MsgProposeSpan {
  option (amino.name) = "giltconsensusv2/gilt/MsgProposeSpan";
  option (cosmos.msg.v1.signer) = "proposer";
  // ID of the span being proposed.
  uint64 span_id = 1;
  // Address of the validator proposing this span.
  string proposer = 2 [ (cosmos_proto.scalar) = "cosmos.AddressString" ];
  // First block number of this span.
  uint64 start_block = 3;
  // Last block number of this span.
  uint64 end_block = 4;
  // Chain ID of the Gilt chain.
  string chain_id = 5;
  // Random seed used for producer selection.
  bytes seed = 6;
  // Address of the validator who generated the seed.
  string seed_author = 7 [ (cosmos_proto.scalar) = "cosmos.AddressString" ];
}

// MsgProposeSpanResponse defines the response for MsgProposeSpan.
message MsgProposeSpanResponse {}

// MsgUpdateParams defines the message for updating gilt module parameters.
message MsgUpdateParams {
  option (cosmos.msg.v1.signer) = "authority";
  option (amino.name) = "giltconsensusv2/gilt/MsgUpdateParams";
  // Address of the governance authority (typically the governance module).
  string authority = 1 [ (cosmos_proto.scalar) = "cosmos.AddressString" ];
  // New parameters to set.
  Params params = 2
      [ (gogoproto.nullable) = false, (amino.dont_omitempty) = true ];
}

// MsgUpdateParamsResponse defines the response for MsgUpdateParams.
message MsgUpdateParamsResponse {}

// MsgBackfillSpans defines the message for backfilling missing spans.
message MsgBackfillSpans {
  option (amino.name) = "giltconsensusv2/gilt/MsgBackfillSpans";
  option (cosmos.msg.v1.signer) = "proposer";
  // Address of the validator proposing the backfill.
  string proposer = 1 [ (cosmos_proto.scalar) = "cosmos.AddressString" ];
  // Chain ID of the Gilt chain.
  string chain_id = 2;
  // Latest span ID known to GiltConsensus.
  uint64 latest_span_id = 3;
  // Latest span ID on the Gilt chain.
  uint64 latest_gilt_span_id = 4;
}

// MsgBackfillSpansResponse defines the response for MsgBackfillSpans.
message MsgBackfillSpansResponse {}

// MsgVoteProducers defines the message for voting on producers.
message MsgVoteProducers {
  option (cosmos.msg.v1.signer) = "voter";
  option (amino.name) = "giltconsensusv2/gilt/MsgVoteProducers";
  // Address of the validator casting the vote.
  string voter = 1 [ (cosmos_proto.scalar) = "cosmos.AddressString" ];
  // ID of the validator casting the vote.
  uint64 voter_id = 2;
  // List of validator IDs being voted for as producers.
  ProducerVotes votes = 3
      [ (gogoproto.nullable) = false, (amino.dont_omitempty) = true ];
}

// MsgVoteProducersResponse defines the response for MsgVoteProducers.
message MsgVoteProducersResponse {}

// MsgSetProducerDowntime defines the message for setting producer downtime.
message MsgSetProducerDowntime {
  option (cosmos.msg.v1.signer) = "producer";
  option (amino.name) = "giltconsensusv2/gilt/MsgSetProducerDowntime";
  // Address of the producer setting their downtime.
  string producer = 1 [ (cosmos_proto.scalar) = "cosmos.AddressString" ];
  // Block range during which the producer will be offline.
  BlockRange downtime_range = 2
      [ (gogoproto.nullable) = false, (amino.dont_omitempty) = true ];
}

// MsgSetProducerDowntimeResponse defines the response for
// MsgSetProducerDowntime.
message MsgSetProducerDowntimeResponse {}
