syntax = "proto3";
package giltconsensusv2.milestone;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "giltconsensusv2/milestone/milestone.proto";

option go_package = "github.com/giltchain/gilt-consensus/x/milestone/types";

// GenesisState defines the milestone module's genesis state.
message GenesisState {
  // Module parameters at genesis.
  Params params = 1
      [ (gogoproto.nullable) = false, (amino.dont_omitempty) = true ];
  // Initial milestones loaded at genesis.
  repeated Milestone milestones = 2
      [ (gogoproto.nullable) = false, (amino.dont_omitempty) = true ];
}
